package mongox

const (
	ASC  = 1
	DESC = -1
)

const (
	Set     = "$set"
	Exists  = "$exists"
	Match   = "$match"
	Group   = "$group"
	Project = "$project"
	Sort    = "$sort"
	Limit   = "$limit"
	Skip    = "$skip"
	Unwind  = "$unwind"
	Push    = "$push"
	Pull    = "$pull"
	First   = "$first"
	Last    = "$last"

	Sum = "$sum"
	Avg = "$avg"
	Min = "$min"
	Max = "$max"
)
